apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: earlywarning-uat-appset
  namespace: openshift-gitops
spec:
  generators:
    - git:
        repoURL: 'http://tfsmaster:8080/tfs/DefaultCollection/MLOps/_git/MLOps'
        revision: second
        directories:
          - path: 'tenants/overlays/uat/earlywarning/*'
  template:
    metadata:
      name: 'earlywarning-{{path.basename}}-uat'
    spec:
      project: 'earlywarning'
      source:
        repoURL: 'http://tfsmaster:8080/tfs/DefaultCollection/MLOps/_git/MLOps'
        targetRevision: second
        path: 'tenants/overlays/uat/earlywarning/{{path.basename}}'
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: 'earlywarning-{{path.basename}}'
      syncPolicy:
        syncOptions:
        - RespectIgnoreDifferences=true
        automated:
          selfHeal: true
          prune: true
      ignoreDifferences:
      - group: project.openshift.io
        kind: Project
        jsonPointers:
          - /metadata/annotations
          - /metadata/labels