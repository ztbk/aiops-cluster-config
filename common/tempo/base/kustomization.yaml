---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-tempo-operator

commonLabels:
  component: tempo-operator

resources:
- namespace.yaml
- operatorgroup.yaml
- subscription.yaml
- ../../installplan-approver-job/base

patches:
- patch: |-
    - op: replace
      path: "/spec/template/spec/containers/0/env"
      value:
      - name: NAMESPACE
        value: "openshift-tempo-operator"
  target:
    kind: Job
    name: installplan-approver
