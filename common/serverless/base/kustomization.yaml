---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-serverless

commonLabels:
  component: openshift-serverless

resources:
- namespace.yaml
- operatorgroup.yaml
- subscription.yaml
- ../../installplan-approver-job/base

patches:
- patch: |-
    - op: replace
      path: "/spec/template/spec/containers/0/env"
      value:
      - name: NAMESPACE
        value: "openshift-serverless"
  target:
    kind: Job
    name: installplan-approver
